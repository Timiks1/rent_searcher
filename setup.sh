#!/bin/bash

# Скрипт для первоначальной настройки проекта

cd "$(dirname "$0")"

echo "🔧 Настройка проекта Telegram Rental Filter"
echo "============================================"
echo ""

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
    echo "✅ Виртуальное окружение создано"
else
    echo "✅ Виртуальное окружение уже существует"
fi

# Активация
echo "🔄 Активация виртуального окружения..."
source venv/bin/activate

# Установка зависимостей
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Создание .env файла
if [ ! -f ".env" ]; then
    echo ""
    echo "📝 Создание файла конфигурации .env..."
    cp env.example .env
    echo "✅ Файл .env создан"
    echo ""
    echo "⚠️  ВАЖНО: Отредактируйте файл .env и заполните:"
    echo "   - TELEGRAM_API_ID (получить на https://my.telegram.org)"
    echo "   - TELEGRAM_API_HASH"
    echo "   - TELEGRAM_PHONE (ваш номер телефона)"
    echo "   - TELEGRAM_CHANNEL (имя канала, например @channel_name)"
    echo ""
    echo "После заполнения .env выполните:"
    echo "  python telegram_client.py  # для первой авторизации"
    echo "  ./start.sh                  # для запуска сервера"
else
    echo "✅ Файл .env уже существует"
    echo ""
    echo "Всё готово! Запустите сервер:"
    echo "  ./start.sh"
fi

echo ""
echo "✨ Настройка завершена!"
