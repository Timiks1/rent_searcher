# 🚀 Быстрый старт

## ✅ Что уже сделано

Виртуальное окружение создано и все зависимости установлены!

## 📋 Следующие шаги

### 1. Настройте конфигурацию

Создайте файл `.env`:

```bash
cp env.example .env
```

Откройте `.env` и заполните:

```env
TELEGRAM_API_ID=your_api_id          # Получить на https://my.telegram.org
TELEGRAM_API_HASH=your_api_hash      # Получить на https://my.telegram.org
TELEGRAM_PHONE=+79991234567           # Ваш номер телефона
TELEGRAM_CHANNEL=@channel_name        # Канал для мониторинга
```

#### Как получить API ключи:

1. Зайдите на https://my.telegram.org
2. Войдите со своим номером телефона
3. Перейдите в "API development tools"
4. Создайте приложение
5. Скопируйте `api_id` и `api_hash`

### 2. Первая авторизация в Telegram

Активируйте виртуальное окружение и запустите тест:

```bash
source venv/bin/activate
python telegram_client.py
```

Введите код подтверждения из Telegram. Создастся файл сессии.

### 3. Запустите веб-сервер

```bash
source venv/bin/activate
python main.py
```

Или используйте удобный скрипт:

```bash
./start.sh
```

### 4. Откройте в браузере

Перейдите по адресу: **http://localhost:8000**

## 🎨 Использование веб-интерфейса

1. **Обновить сообщения** - загрузить последние объявления из Telegram
2. **Установить фильтры**:
   - Минимальная/максимальная цена
   - Локация (город, район, метро)
3. **Применить фильтры** - показать подходящие объявления
4. **Открыть в Telegram** - перейти к оригинальному сообщению

## 📡 API Endpoints

- `POST /api/fetch-messages` - загрузить сообщения
- `GET /api/messages?min_price=X&max_price=Y&location=Z` - получить отфильтрованные
- `GET /api/stats` - статистика
- `GET /api/channel-info` - информация о канале

## 🐛 Troubleshooting

### Ошибка при авторизации
- Проверьте правильность API_ID и API_HASH
- Номер телефона должен быть в формате +7...

### Не загружаются сообщения
- Убедитесь, что канал публичный или вы его участник
- Проверьте имя канала (должно начинаться с @)

### Порт занят
- Измените PORT в .env файле

## 📞 Поддержка

Все подробности в `README.md`
